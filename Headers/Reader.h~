#ifndef READER_H
#define READER_H

#include <vector>
#include <string>

class GPS {

        //Attributs
        std::string result;         //Buffer des données GPS, contient X,Y,Z et CovX,CovY et CovZ
        std::string topic_name;
        
        //Fonctions
        public :
                //Constructeur et Destructeur
                Reader(std::string name, std::topic);
                ~Reader();
                
                //Afichage
                void Display();


};

#endif
