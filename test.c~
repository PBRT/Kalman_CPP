#include <stdio.h>
#include <stdlib.h>

int main(){

	int it = 0;

	while(1){
		system("rostopic pub -r 1 foo std_msgs/String \"GPS data\"");
		system("rostopic pub -r 1 foo2 std_msgs/String \"QR data\"");
		system("rostopic pub -r 1 foo3 std_msgs/String \"ODOM data\"");
		system("rostopic pub -r 1 foo4 std_msgs/String \"IMU data\"");
	
		
	}

	return 0;
}
